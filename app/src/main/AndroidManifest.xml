<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.vl.myservice">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <service
                android:name=".service.MyService"
                android:enabled="true"
                android:exported="false">
        </service>

        <activity android:name=".activity.LinearHorizontalLayoutActivity">
        </activity>
        <activity android:name=".activity.RelativeLayoutActivity">
        </activity>
        <activity android:name=".activity.LinearVerticalLayoutActivity">
        </activity>
        <activity android:name=".activity.ConstraintLayoutActivity">
        </activity>
    </application>

</manifest>